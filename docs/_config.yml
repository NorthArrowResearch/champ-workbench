# Site settings
title: CHaMP Workbench
description: > # this means to ignore newlines until "baseurl:"
  Some site description will go here
url: https://northarrowresearch.github.io/champ-workbench/
baseurl: ""

# Build settings
markdown: kramdown
highlighter: rouge

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"


layouts_dir:  src/_layouts
includes_dir: src/_includes

exclude:
  - src
  - LICENSE
  - README.md
  - _config.yml